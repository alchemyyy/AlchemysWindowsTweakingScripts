idfk why but winaerotweaker doesnt seem to have a launch parameter to import a file and apply it automatically.

so when the program launches go to file->import and export, then do what it says. the ini file is right here in this directory.